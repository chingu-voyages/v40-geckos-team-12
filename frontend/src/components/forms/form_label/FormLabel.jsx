import React from "react";
import { LabelValueContainer, StyledLabel, StyledInput } from "../Form.styled";

const FormLabel = ({ text }) => {
  return <StyledLabel>{text}</StyledLabel>;
};

export default FormLabel;
